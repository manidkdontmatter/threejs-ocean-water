{
  "name": "web-ocean-water-workspace",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace web-ocean-water-demo",
    "preview": "npm run preview --workspace web-ocean-water-demo",
    "build": "npm run build --workspace web-ocean-water && npm run build --workspace web-ocean-water-demo",
    "build:lib": "npm run build --workspace web-ocean-water",
    "build:demo": "npm run build --workspace web-ocean-water-demo",
    "typecheck": "npm run typecheck --workspace web-ocean-water && npm run typecheck --workspace web-ocean-water-demo",
    "test:follow-snap": "npm run test:follow-snap --workspace web-ocean-water-demo"
  }
}
